/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ReviewScrollbuttonsDirective } from './review-scrollbuttons.directive';

describe('Directive: ReviewScrollbuttons', () => {
  it('should create an instance', () => {
    const directive = new ReviewScrollbuttonsDirective();
    expect(directive).toBeTruthy();
  });
});
