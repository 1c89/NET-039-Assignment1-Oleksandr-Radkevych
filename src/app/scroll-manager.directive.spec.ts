/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ScrollManager } from './scroll-manager.directive';
import { ScrollSection } from './scroll-section.directive';

describe('Directive: ScrollManagerDirective', () => {
  it('should create an instance', () => {
    //TODO how to initialize DI of ScrollManager with parameters
    // const directive = new ScrollManager(new ScrollSelection());
    // expect(directive).toBeTruthy();
  });
});
